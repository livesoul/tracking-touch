import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "th.co.xsense.touch",
  appName: "Tracking Touch",
  webDir: "build",
  bundledWebRuntime: false,
};

export default config;
